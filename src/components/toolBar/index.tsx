import React from "react";
import "./index.less";
import ToolPanel from "./tool";

const Toolbar = () => {
    return (
        <div className="toolbar">
            <ToolPanel />
        </div>
    );
};

export default Toolbar;
